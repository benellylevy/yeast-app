
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { ScrollArea } from "@/components/ui/scroll-area";
import { FileText } from "lucide-react";

export default function FeedbackLogTable({ feedbackLogs }) {
  return (
    <Card className="bg-gray-900 border-cyan-400 border-opacity-30 shadow-lg shadow-cyan-400/20 h-full">
      <CardHeader className="pb-4">
        <CardTitle className="text-cyan-400 font-mono flex items-center gap-2">
          <FileText className="w-5 h-5" />
          🧾 FEEDBACK LOG
        </CardTitle>
      </CardHeader>
      <CardContent className="p-0">
        <ScrollArea className="h-96">
          <Table>
            <TableHeader>
              <TableRow className="border-cyan-400/20 hover:bg-gray-800/50">
                <TableHead className="text-cyan-400 font-mono">#</TableHead>
                <TableHead className="text-cyan-400 font-mono">VAL</TableHead>
                <TableHead className="text-cyan-400 font-mono">TEMP_SEND</TableHead>
                <TableHead className="text-cyan-400 font-mono">TIME</TableHead>
              </TableRow>
            </TableHeader>
            <TableBody>
              {feedbackLogs.map((log) => (
                <TableRow 
                  key={log.id} 
                  className="border-cyan-400/10 hover:bg-gray-800/30 transition-colors"
                >
                  <TableCell className="font-mono text-gray-300">
                    {log.sequence_number}
                  </TableCell>
                  <TableCell className="font-mono text-lime-400">
                    {log.feedback_value?.toFixed(3)}
                  </TableCell>
                  <TableCell className="font-mono text-orange-400">
                    {log.temperature_send?.toFixed(1)}°C
                  </TableCell>
                  <TableCell className="font-mono text-gray-400 text-xs">
                    {new Date(log.timestamp).toLocaleTimeString()}
                  </TableCell>
                </TableRow>
              ))}
              {feedbackLogs.length === 0 && (
                <TableRow>
                  <TableCell colSpan={4} className="text-center text-gray-500 py-8 font-mono">
                    NO DATA AVAILABLE
                  </TableCell>
                </TableRow>
              )}
            </TableBody>
          </Table>
        </ScrollArea>
      </CardContent>
    </Card>
  );
}
